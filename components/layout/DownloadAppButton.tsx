/**
 * DownloadAppButton — kept for backward-compatibility.
 * The implementation has been replaced by InstallPrompt.
 * Any existing imports of `DownloadAppButton` will transparently
 * use the new production-grade component.
 */
export { InstallPrompt as DownloadAppButton } from "./InstallPrompt";
